# Build output, vendored trees and the separate Docusaurus site.
js/
dist/
build/
node_modules/
vendor/
coverage/
coverage-frontend/
docs/
docusaurus/
website/
playwright-report/
test-results/
*.min.*
# Written by the translation workflow — a formatter here would fight its own
# generator on every run.
l10n/
# GENERATED by `npm run icons:opengemeenten` — 227 extracted SVG paths. Formatting
# it is work the next generator run undoes. eslint.config.js already ignores this
# same file for the same reason.
src/icons/openGemeentenIcons.js
