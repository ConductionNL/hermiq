legacy-peer-deps=true

# Supply-chain hardening: reject any npm package published less than 24h ago.
# Override per-install for fresh @conduction/* releases with --min-release-age=0.
min-release-age=0
